# Server Configuration
PORT=3000
NODE_ENV=development

# Default MongoDB Connection (optional - connections can also be managed via UI)
# MONGO_URI=mongodb://localhost:27017

# Connections file path (relative to backend root)
CONNECTIONS_FILE=./data/connections.json

# Pipelines storage file path
PIPELINES_FILE=./data/pipelines.json
